"""cod-ag deterministic script layer.

Every module here is stdlib-only and runs identically on Windows, macOS and
Linux. The agents call this package through ``scripts/codag.py``.
"""

__version__ = "0.1.0"
